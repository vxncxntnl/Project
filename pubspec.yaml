name: communityserviceplatform
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.1
  intl: ^0.20.2
  url_launcher: ^6.3.1
  google_fonts: ^3.0.1  # Added google_fonts package

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/COC-PHINMA.jpg
